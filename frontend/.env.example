# Leer lassen fuer lokale Entwicklung (nutzt den Vite-Dev-Proxy aus vite.config.js).
# Fuer ein Produktions-Deployment (z. B. Frontend auf Vercel, Backend separat
# auf Render) auf die volle Backend-Origin setzen:
# VITE_API_BASE_URL=https://triage-dashboard-api.onrender.com
VITE_API_BASE_URL=
